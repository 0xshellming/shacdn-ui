---
title:  抽屉
description:  扩展 Dialog 组件，以显示补充屏幕主要内容的内容。
component: true
links:
  doc: https://www.radix-ui.com/docs/primitives/components/dialog
  api: https://www.radix-ui.com/docs/primitives/components/dialog#api-reference
---

<ComponentPreview name="sheet-demo" description="抽屉组件。" />

## 安装

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">手动</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn-ui@latest add sheet
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>安装以下依赖项：</Step>

```bash
npm install @radix-ui/react-dialog
```

<Step>复制以下代码并粘贴到你的项目中。</Step>

<ComponentSource name="sheet" />

<Step>更新导入路径以匹配你的项目设置。</Step>

</Steps>

</TabsContent>

</Tabs>

### 用法

```tsx
import {
  Sheet,
  SheetContent,
  SheetDescription,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from "@/components/ui/sheet"
```

```tsx
<Sheet>
  <SheetTrigger>打开</SheetTrigger>
  <SheetContent>
    <SheetHeader>
      <SheetTitle>你绝对确定吗？</SheetTitle>
      <SheetDescription>
        这个操作无法撤消。这个操作会永久删除你的账户，并且把你的数据从服务器移除。
      </SheetDescription>
    </SheetHeader>
  </SheetContent>
</Sheet>
```

## 示例

### 侧边栏

使用 `<SheetContent />` 中的 `side` 属性来指明该组件将在屏幕的哪一侧出现。其值可以是 `top`、`right`、`bottom` 或 `left`。

<ComponentPreview name="sheet-side" />

### 尺寸

你可以通过 CSS 类调整抽屉的大小：

```tsx {3}
<Sheet>
  <SheetTrigger>打开</SheetTrigger>
  <SheetContent className="w-[400px] sm:w-[540px]">
    <SheetHeader>
      <SheetTitle>你绝对确定吗？</SheetTitle>
      <SheetDescription>
        这个操作无法撤消。这个操作会永久删除你的账户，并且把你的数据从服务器移除。
      </SheetDescription>
    </SheetHeader>
  </SheetContent>
</Sheet>
```