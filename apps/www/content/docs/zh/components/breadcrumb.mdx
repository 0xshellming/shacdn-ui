---
title: Breadcrumb
description: Displays the path to the current resource using a hierarchy of links.
component: true
---

## 安装

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">手动</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn-ui@latest add breadcrumb
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>复制并将以下代码粘贴到您的项目中。</Step>

<ComponentSource name="breadcrumb" />

<Step>更新 import 路径以匹配您的项目设置。</Step>

</Steps>

</TabsContent>

</Tabs>

## 用法

```tsx
import {
  Breadcrumb,
  BreadcrumbItem,
  BreadcrumbLink,
  BreadcrumbList,
  BreadcrumbPage,
  BreadcrumbSeparator,
} from "@/components/ui/breadcrumb"
```

```tsx
<Breadcrumb>
  <BreadcrumbList>
    <BreadcrumbItem>
      <BreadcrumbLink href="/">首页</BreadcrumbLink>
    </BreadcrumbItem>
    <BreadcrumbSeparator />
    <BreadcrumbItem>
      <BreadcrumbLink href="/components">组件</BreadcrumbLink>
    </BreadcrumbItem>
    <BreadcrumbSeparator />
    <BreadcrumbItem>
      <BreadcrumbPage>面包屑</BreadcrumbPage>
    </BreadcrumbItem>
  </BreadcrumbList>
</Breadcrumb>
```

## 案例

### 自定分隔符

使用自定义组件作为 `<BreadcrumbSeparator />` 的 `children` 来创建自定义分隔符。

<ComponentPreview
  name="breadcrumb-separator"
  description="带自定义分隔符的面包屑"
/>

```tsx showLineNumbers {1,10-12}
import { Slash } from "lucide-react"

...

<Breadcrumb>
  <BreadcrumbList>
    <BreadcrumbItem>
      <BreadcrumbLink href="/">首页</BreadcrumbLink>
    </BreadcrumbItem>
    <BreadcrumbSeparator>
      <Slash />
    </BreadcrumbSeparator>
    <BreadcrumbItem>
      <BreadcrumbLink href="/components">组件</BreadcrumbLink>
    </BreadcrumbItem>
  </BreadcrumbList>
</Breadcrumb>
```

---

### 下拉菜单

您可以将 `<BreadcrumbItem />` 与 `<DropdownMenu />` 结合使用，在面包屑中创建一个下拉菜单。

<ComponentPreview
  name="breadcrumb-dropdown"
  className="[&_.preview]:p-2"
  description="带下拉菜单的面包屑"
/>

```tsx showLineNumbers {1-6,11-21}
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu"

...

<BreadcrumbItem>
  <DropdownMenu>
    <DropdownMenuTrigger className="flex items-center gap-1">
      组件
      <ChevronDownIcon />
    </DropdownMenuTrigger>
    <DropdownMenuContent align="start">
      <DropdownMenuItem>文档</DropdownMenuItem>
      <DropdownMenuItem>主题</DropdownMenuItem>
      <DropdownMenuItem>GitHub</DropdownMenuItem>
    </DropdownMenuContent>
  </DropdownMenu>
</BreadcrumbItem>
```

---

### 折叠

当面包屑过长时，我们提供了 `<BreadcrumbEllipsis />` 组件来显示折叠状态。

<ComponentPreview
  name="breadcrumb-ellipsis"
  className="[&_.preview]:p-2"
  description="显示折叠状态的面包屑"
/>

```tsx showLineNumbers {1,9}
import { BreadcrumbEllipsis } from "@/components/ui/breadcrumb"

...

<Breadcrumb>
  <BreadcrumbList>
    {/* ... */}
    <BreadcrumbItem>
      <BreadcrumbEllipsis />
    </BreadcrumbItem>
    {/* ... */}
  </BreadcrumbList>
</Breadcrumb>
```

---

### 链接组件

要从您的路由库中使用自定义链接组件，您可以在 `<BreadcrumbLink />` 上使用 `asChild` 属性。

<ComponentPreview
  name="breadcrumb-link"
  description="带自定义链接组件的面包屑"
/>

```tsx showLineNumbers {1,8-10}
import { Link } from "next/link"

...

<Breadcrumb>
  <BreadcrumbList>
    <BreadcrumbItem>
      <BreadcrumbLink asChild>
        <Link href="/">首页</Link>
      </BreadcrumbLink>
    </BreadcrumbItem>
    {/* ... */}
  </BreadcrumbList>
</Breadcrumb>
```

---

### 响应式

以下是一个响应式面包屑的示例，它将 `<BreadcrumbItem />` 与 `<BreadcrumbEllipsis />`、`<DropdownMenu />` 和 `<Drawer />` 结合使用。

它在桌面上显示一个下拉菜单，在移动设备上显示一个抽屉。

<ComponentPreview
  name="breadcrumb-responsive"
  className="[&_.preview]:p-2"
  description="响应式面包屑. 它在桌面上显示一个下拉菜单，在移动设备上显示一个抽屉。"
/>