---
title: Button 按钮
description: 显示一个按钮或类似按钮的组件。
featured: true
component: true
---

<ComponentPreview name="button-demo" description="一个按钮" />

## 安装

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">命令行</TabsTrigger>
  <TabsTrigger value="manual">手动</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn-ui@latest add button
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>安装以下依赖项：</Step>

```bash
npm install @radix-ui/react-slot
```

<Step>将以下代码复制并粘贴到你的项目中。</Step>

<ComponentSource name="button" />

<Step>更新导入路径以匹配你的项目设置。</Step>

</Steps>

</TabsContent>

</Tabs>

## 使用

```tsx
import { Button } from "@/components/ui/button"
```

```tsx
<Button variant="outline">按钮</Button>
```

## 链接

你可以使用`buttonVariants`帮助器创建一个看起来像按钮的链接。

```tsx
import { buttonVariants } from "@/components/ui/button"
```

```tsx
<Link className={buttonVariants({ variant: "outline" })}>点击这里</Link>
```

或者，你可以设置`asChild`参数并嵌套链接组件。

```tsx
<Button asChild>
  <Link href="/login">登录</Link>
</Button>
```

## 示例

###主要的

<ComponentPreview name="button-demo" description="一个主要的按钮" />

### 次要的

<ComponentPreview name="button-secondary" description="一个次要的按钮" />

### 破坏性的

<ComponentPreview
  name="button-destructive"
  description="一个破坏性的按钮"
/>

### 轮廓

<ComponentPreview
  name="button-outline"
  description="使用轮廓变体的按钮。"
/>

### 幽灵

<ComponentPreview
  name="button-ghost"
  description="使用幽灵变体的按钮"
/>

### 链接

<ComponentPreview
  name="button-link"
  description="使用链接变体的按钮。"
/>

### 图标

<ComponentPreview name="button-icon" description="一个图标按钮" />

### 带有图标的

<ComponentPreview name="button-with-icon" description="一个带有图标的按钮" />

### 加载中

<ComponentPreview
  name="button-loading"
  description="一个带有加载状态的按钮。"
/>

### 作为子元素

<ComponentPreview
  name="button-as-child"
  description="一个包裹自定义链接组件的按钮"
/>