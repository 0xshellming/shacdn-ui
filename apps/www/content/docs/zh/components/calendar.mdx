---
title: 日历
description: 一个允许用户输入和编辑日期的日期字段组件。
component: true
links:
  doc: https://react-day-picker.js.org
---

<ComponentPreview name="calendar-demo" />

## 关于

`Calendar` 组件基于 [React DayPicker](https://react-day-picker.js.org) 构建。

## 安装

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">手动</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn-ui@latest add calendar
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>安装以下依赖项：</Step>

```bash
npm install react-day-picker date-fns
```

<Step>将 `Button` 组件添加到您的项目中。</Step>

`Calendar` 组件使用 `Button` 组件。确保已在您的项目中安装它。

<Step>将以下代码复制并粘贴到您的项目中。</Step>

<ComponentSource name="calendar" />

<Step>更新导入路径以匹配您的项目设置。</Step>

</Steps>

</TabsContent>

</Tabs>

## 用法

```tsx
import { Calendar } from "@/components/ui/calendar"
```

```tsx
const [date, setDate] = React.useState<Date | undefined>(new Date())

return (
  <Calendar
    mode="single"
    selected={date}
    onSelect={setDate}
    className="rounded-md border"
  />
)
```

有关更多信息，请参见 [React DayPicker](https://react-day-picker.js.org) 文档。

## 日期选择器

您可以使用 `<Calendar>` 组件来构建日期选择器。有关更多信息，请参见 [日期选择器](/docs/components/date-picker) 页面。

## 示例

### 表单

<ComponentPreview name="calendar-form" />