---
title: 徽章
description: 显示徽章或看起来像是徽章的组件。
component: true
---

<ComponentPreview name="badge-demo" description="一个徽章" />

## 安装

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">手动</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn-ui@latest add badge
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>复制并粘贴以下代码到你的项目中。</Step>

<ComponentSource name="badge" />

<Step>更新导入路径以匹配你的项目设置.</Step>

</Steps>

</TabsContent>

</Tabs>

## 使用

```tsx
import { Badge } from "@/components/ui/badge"
```

```tsx
<Badge variant="outline">徽章</Badge>
```

### 链接

你可以使用`badgeVariants`帮助器创建一个看起来像徽章的链接。

```tsx
import { badgeVariants } from "@/components/ui/badge"
```

```tsx
<Link className={badgeVariants({ variant: "outline" })}>徽章</Link>
```

## 示例

### 默认

<ComponentPreview name="badge-demo" description="一个徽章" />

---

### 次要

<ComponentPreview
  name="badge-secondary"
  description="使用次要作为变体的徽章。"
/>

---

### 轮廓

<ComponentPreview
  name="badge-outline"
  description="使用轮廓作为变体的徽章。"
/>

---

### 毁灭性的

<ComponentPreview name="badge-destructive" description="一个具有破坏性的徽章。" />