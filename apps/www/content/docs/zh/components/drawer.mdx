---
title: 抽屉
description: React 的抽屉组件。
component: true
links:
  doc: https://github.com/emilkowalski/vaul
---

<ComponentPreview name="drawer-demo" />

## 关于

抽屉建立在 [emilkowalski\_](https://twitter.com/emilkowalski_) 的 [Vaul](https://github.com/emilkowalski/vaul) 的基础之上。

## 安装

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">手动</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn-ui@latest add drawer
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>安装以下依赖项：</Step>

```bash
npm install vaul
```

<Step>将以下代码复制粘贴到项目中。</Step>

<ComponentSource name="drawer" />

<Step>更新导入路径以匹配项目的设置。</Step>

</Steps>

</TabsContent>

</Tabs>

## 使用

```tsx showLineNumbers
import {
  Drawer,
  DrawerClose,
  DrawerContent,
  DrawerDescription,
  DrawerFooter,
  DrawerHeader,
  DrawerTitle,
  DrawerTrigger,
} from "@/components/ui/drawer"
```

```tsx showLineNumbers
<Drawer>
  <DrawerTrigger>打开</DrawerTrigger>
  <DrawerContent>
    <DrawerHeader>
      <DrawerTitle>您确定吗？</DrawerTitle>
      <DrawerDescription>此操作不可撤消。</DrawerDescription>
    </DrawerHeader>
    <DrawerFooter>
      <Button>提交</Button>
      <DrawerClose>
        <Button variant="outline">取消</Button>
      </DrawerClose>
    </DrawerFooter>
  </DrawerContent>
</Drawer>
```

## 示例

### 自适应对话框

可以将 `Dialog` 和 `Drawer` 组件结合使用来创建自适应对话框。这会在桌面上呈现一个 `Dialog` 组件，在移动设备上呈现一个 `Drawer` 组件。

<ComponentPreview name="drawer-dialog" />