---
title: 警报
description: 显示醒目的用户提示。
component: true
---

<ComponentPreview
  name="alert-demo"
  description="带图标、标题和描述的警报。标题是“注意！”，描述是“你可以使用 cli 向你的应用程序中添加组件。”。"
/>

## 安装

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">手动</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn-ui@latest add alert
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>将以下代码复制粘贴到你的项目中。</Step>

<ComponentSource name="alert" />

<Step>更新导入路径以匹配你的项目设置。</Step>

</Steps>

</TabsContent>

</Tabs>

## 用法

```tsx
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert"
```

```tsx
<Alert>
  <Terminal className="h-4 w-4" />
  <AlertTitle>注意！</AlertTitle>
  <AlertDescription>
    你可以使用 cli 向你的应用程序中添加组件和依赖项。
  </AlertDescription>
</Alert>
```

## 示例

### 默认

<ComponentPreview
  name="alert-demo"
  description="带图标、标题和描述的警报。标题是“注意！”，描述是“你可以使用 cli 向你的应用程序中添加组件。”。"
/>

### 销毁

<ComponentPreview
  name="alert-destructive"
  description="带销毁变体的警报。标题是“删除此项？”，描述是“此操作不可撤销。”。"
/>